# modules : a built-in module is a module that is compiled into the Python
# interpreter
# modules are files containing Python code that define functions, classes
# and variables
# that we want to include in our application.
# Examples of popular built-in modules in Python are os, sys, math, random,
# json,time etc.id
import math
import random

random_number = random.randint(1, 10)
random_shuffle = random.shuffle([1, 2, 3, 4, 5])
